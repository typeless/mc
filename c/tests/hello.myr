use sys

type File = void#

//extern const stdout : File

//extern const putchar : (c : int -> int)
//extern const fputs : (s : byte#, fp : File -> int)

//const put = {s : byte[:]
//	fputs(&s[0], stdout)
//}

const foo = {x : int, y : int -> int32
	var xs : int[3] = [1, 2, 3]
	var v = (1, 2, 3)
	match v
	| (1, 2, a):
		-> ((x + a) : int32)
	| (b, 5, 6):
		-> ((b + y) : int32)
	|_:
		-> (x +y : int32)
	;;
}

const bar = {
	var a = 12345
	var b = 456
	-> {;-> a + b }() + {; -> a * b}()
}

const main = {
	var x, y
	var a = foo(200, 300)
	var b = 123
	sys.write(2, "hello world\n")
	(x, y) = (10, 20)
	std.put("byte\n")
	//var f = {; -> {;->b} }
}
